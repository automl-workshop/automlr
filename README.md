# Hello World

A framework-free workshop site deployed with Cloudflare Workers Builds.

Pushes to `main` automatically deploy the repository's static assets using the
configuration in `wrangler.jsonc`. The production site is available at
<https://automlr.com>.

Only files in `public/` are deployed as website assets. Proposal sources and
other project files remain private to the repository or local workspace.
